@inherits LayoutComponentBase
@using BookingService.Web.Features.Auth

<div class="min-h-screen bg-slate-50 text-slate-900">
    <header class="border-b border-slate-200 bg-white">
        <nav class="mx-auto flex max-w-7xl items-center justify-between px-6 py-4">
            <a href="/" class="text-lg font-bold tracking-tight text-indigo-600">Booking Service</a>
            <AuthorizeView Context="authContext">
                <Authorized>
                    <div class="flex items-center gap-4 text-sm">
                        <a href="/Rooms" class="font-semibold text-slate-600 hover:text-indigo-600">Rooms</a>
                        <a href="/BookRoom" class="font-semibold text-slate-600 hover:text-indigo-600">Book a Room</a>
                        <a href="/MyBookings" class="font-semibold text-slate-600 hover:text-indigo-600">My Bookings</a>
                        <AuthorizeView Roles="@AuthRoles.Admin">
                            <a href="/AllBookings" class="font-semibold text-slate-600 hover:text-indigo-600">All Bookings</a>
                        </AuthorizeView>
                        <span class="text-slate-600">@authContext.User.Identity?.Name</span>
                        <button type="button" @onclick="LogoutAsync"
                                class="rounded-lg border border-slate-300 px-3 py-2 font-semibold text-slate-700 transition hover:bg-slate-100">
                            Sign out
                        </button>
                    </div>
                </Authorized>
            </AuthorizeView>
        </nav>
    </header>

    <main>
        @Body
    </main>
</div>

<div id="blazor-error-ui" data-nosnippet>
    An unhandled error has occurred.
    <a href="." class="reload">Reload</a>
    <span class="dismiss">🗙</span>
</div>

@code {
    [Inject] private IJSRuntime Js { get; set; } = default!;
    [Inject] private NavigationManager Navigation { get; set; } = default!;

    private async Task LogoutAsync()
    {
        await Js.InvokeAsync<bool>("bookingServiceAuth.logout");
        Navigation.NavigateTo("/Auth/Login", forceLoad: true);
    }
}